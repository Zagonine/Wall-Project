{% extends "::base.html.twig" %}

{% block title %}The Wall Project{% endblock %}

{% block body %}

    {% for post in posts %}
        <div>
            <small class="text-muted">{{ post.pseudo }}, le {{ post.datePosted | date("m/d/Y") }} | Catégorie(s) :
                {% for category in post.categories %}
                    {{ category.name }}{% if not loop.last %},{% endif %}
                {% endfor %}
            </small>
            <br>
            {{ post.content }}
        </div>
        {% if not loop.last %}
            <hr>
        {% endif %}
    {% endfor %}

{% endblock %}
